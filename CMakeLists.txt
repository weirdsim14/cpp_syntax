# Specify the minimum version for CMake
cmake_minimum_required(VERSION 3.15)

# Define the project name and version
project(Syntax VERSION 1.0)

# Add and executable target with a name
add_executable(storage_class storage_class.cpp)

